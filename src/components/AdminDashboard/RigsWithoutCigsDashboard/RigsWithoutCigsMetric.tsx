import { Box, Typography } from "@mui/material";
import { ReactNode } from "react";

export default function RigsWithoutCigsMetric(): ReactNode {
  return (
    <Box>
      <Typography>Rigs Without Cigs Metrics</Typography>
    </Box>
  );
}
